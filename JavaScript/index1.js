let a = [10, 25, 20, 40.5, 50, 60]
console.log(a.length);
a.unshift(); // added to the first index
a.pop(); // remove element from the last
a.sort(); // it will sort the order
a.push(70); // add element in thi last
a.reverse();
a.toString(); // convert array to string
console.log(a);


// Creating an array using literals
let myArray = [1, 2, 'apple', 'orange', true];

// Displaying elements of the array
console.log("Elements of the array are:" + myArray);

// Creating an array using the array constructor
let myArray1 = new Array(3); // Creates an array with length 3

// Adding elements to the array
myArray1[0] = 'apple';
myArray1[1] = 'banana';
myArray1[2] = 'orange';

console.log("Elements of the array:" + myArray1);





